<Window x:Class="Nexus.App.Addbook"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Nexus.App"
        mc:Ignorable="d"
        Title="Addbook" Height="450" Width="800" SizeToContent="WidthAndHeight" Style="{StaticResource DialogBase}" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="ID:" IsEnabled="False" />
        <TextBox IsHitTestVisible="False" IsEnabled="False" Grid.Column="1" Text="{Binding ID, Mode=OneWay}"/>
        <Label Name="LblName" Content="_Name:" Grid.Row="1" Target="{Binding ElementName=TxtName}"/>
        <TextBox x:Name="TxtName" Grid.Row="1" Grid.Column="1" Text="{Binding BookName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
        <Label Name="LblAuthor" Content="_Author:" Grid.Row="2" Target="{Binding ElementName=TxtAuthor}"/>
        <TextBox x:Name="TxtAuthor" Grid.Row="2" Grid.Column="1" Text="{Binding Author, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="_OK" Name="BtnOK" IsDefault="True"/>
            <Button Content="_Cancel" Name="BtnCancel" IsCancel="True" Margin="10,0,2,0"/>
        </StackPanel>
    </Grid>
</Window>

   
