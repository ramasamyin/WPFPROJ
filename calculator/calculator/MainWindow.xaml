<Window x:Class="calculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:calculator"
        mc:Ignorable="d"
        Title="Calculator" Height="450" Width="350"
        KeyDown="Window_KeyDown"
        TextInput="Window_TextInput">

    <Window.Resources>
        <Style TargetType="Button">
            <EventSetter Event="Click" Handler="Button_Click"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="AliceBlue"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Name="ExpressionText" FontSize="20" HorizontalAlignment="Right" VerticalAlignment="Center" Height="40"/>

        <TextBox Name="DisplayText" Grid.Row="1" FontSize="28" Height="50" HorizontalAlignment="Stretch"
                 VerticalAlignment="Center" IsReadOnly="True" TextAlignment="Right"/>

        <UniformGrid Grid.Row="2" Rows="5" Columns="4" Margin="0,10,0,0">
            <Button Content="C"/>
            <Button Content="CE"/>
            <Button Content="⌫"/>
            <Button Content="/"/>

            <Button Content="7"/>
            <Button Content="8"/>
            <Button Content="9"/>
            <Button Content="*"/>

            <Button Content="4"/>
            <Button Content="5"/>
            <Button Content="6"/>
            <Button Content="-"/>

            <Button Content="1"/>
            <Button Content="2"/>
            <Button Content="3"/>
            <Button Content="+"/>

            <Button Content="+/-"/>
            <Button Content="0"/>
            <Button Content="."/>
            <Button Content="=" Background="Green"/>
        </UniformGrid>
    </Grid>
</Window>
